this code should send a network proticol to a bunch of IPs it has gotten and have it be able to set itself up via data transferr being itself but also putting itself
into the startup* it should have its own ip scanning tool  
